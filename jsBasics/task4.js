//4.1

let radiusCircle = 5;
let areaCircle = Math.PI * Math.pow(radiusCircle, 2); 

console.log(`Площа кола з радіусом ${radiusCircle} = ${areaCircle.toFixed(2)}`);


//4.2
let length = 8; 
let width = 4; 
let areaRectangle = length * width; 

console.log(`Площа прямокутника з довжиною ${length} та шириною ${width} = ${areaRectangle.toFixed(2)}`);

//4.3

let radius = 3; 
let height = 7; 
let volume = Math.PI * Math.pow(radius, 2) * height;

console.log(`Об'єм циліндра з радіусом ${radius} та висотою ${height} = ${volume.toFixed(2)}`);
